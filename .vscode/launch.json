{
    "configurations": [
        {
            "name": "Attach to Edge",
            "port": 8800,
            "request": "attach",
            "type": "msedge",
            "webRoot": "${workspaceFolder}"
        }
    
    ]
}